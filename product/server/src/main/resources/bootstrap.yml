spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      profile: test

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
